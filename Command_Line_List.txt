git status = list unstaged files that have been changed

git diff = list changes

git log = list recent commits

git add . = stage all changed file
git add #filename = stage a specific file

git commit -m "#title" = commit files that have staged in local repository
git push -u origin master = push to GitHub repository

